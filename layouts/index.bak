{{ partial "header.html" . }}

{{ $pageSize := .Site.Params.indexPaginatorPageSize | default 2  }}
{{ $postLimit := .Site.Params.indexPostLimit | default 50  }}
{{ $paginator := .Paginate (first $postLimit (where .Site.Pages "Section" "!=" "index")) $pageSize }}


<!-- Biography Section -->
{{ range where .Site.Pages "Section" "index" }}
{{ if eq .Title "about" }}
<section id="about" class="index-section">
    <div class="container">
        {{ partial "index/about.html" . }}
    </div>
</section>
{{ end }}
{{ end }}

{{ $last := add $paginator.NumberOfElements -1  }}
{{ range $i, $page := $paginator.Pages }}
  <section class="index-post">
  {{ partial "content-header.html" . }}
    <section class="content">{{ .Summary }}
    {{ if .Truncated }}
    <span class="read-more">
      <a href="{{ .RelPermalink  }}">Read More &hellip;</a>
    </span>
    {{ end  }}
    </section>
  </section>
    {{ if lt $i $last }}<hr class="index-post">{{ end }}
{{ end  }}
{{ template "_internal/pagination.html" . }}
{{ partial "footer.html" . }}
